services:
  web:
    build: .
    expose:
      - "8000"
    env_file:
      - .env
    restart: always

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "4400:80"
    depends_on:
      - web
    restart: always